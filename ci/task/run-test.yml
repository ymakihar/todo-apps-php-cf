---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: atoum/atoum


inputs:
  - name: resource-todo-app
    
params:
  TEST_TOKEN:

run:
  path: bash
  args: 
  - -c 
  - |
    cd resource-todo-*/
    composer config -g github-oauth.github.com $TEST_TOKEN
    composer install
    atoum -ulr -d test ++verbose
